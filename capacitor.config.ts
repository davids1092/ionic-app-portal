import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'portal-key-pago',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
